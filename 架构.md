# Banner 指纹识别系统 — 架构设计

> 本文档描述技术选型、总体架构、模块拆解与目录划分，不含具体实现代码。

---

## 一、设计目标与原则

| 目标 | 落地原则 |
| --- | --- |
| 30 分钟内可完成 | 依赖最小化、结构扁平、不做过度抽象 |
| 生产级部署 | 多阶段构建、非 root、只读根文件系统、能力裁剪、真实健康检测 |
| 规则与代码解耦 | 识别规则以配置文件承载，程序只做「加载 + 匹配」 |
| 认不出不崩 | 任何输入都返回结构体，`unknown` 是一等公民 |
| 可自证 | 注释、README、单测、示例数据齐备 |

**核心取舍**：优先使用 Go 标准库，把风险留给业务逻辑而不是依赖下载。仅在规则解析上引入 YAML 库（若网络受限可退化为 JSON，标准库即可解析）。

---

## 二、技术栈选型

| 层次 | 选型 | 理由 |
| --- | --- | --- |
| 语言 | Go 1.23 | 题目要求；标准库自带 HTTP/JSON，编译为静态单文件，天然适合容器交付 |
| HTTP 服务 | 标准库 `net/http` + `http.ServeMux` | Go 1.22+ 的 ServeMux 已支持 `"POST /fingerprint"` 方法路由，无需引入 gin/chi |
| 日志 | 标准库 `log/slog` | 结构化日志，零依赖，JSON/Text handler 可切换 |
| 配置 | 标准库 `encoding/json` + `os.Getenv` | 环境变量注入，12-Factor 风格 |
| 规则格式 | YAML（`gopkg.in/yaml.v3`） | 表达力强、可读性好；若依赖获取受限则降级为 JSON（规则结构不变） |
| 规则分发 | `go:embed` 内置默认规则 + 外部文件挂载覆盖 | 既开箱即用，又支持不改镜像换规则 |
| 测试 | 标准库 `testing` + `testdata/` | 表驱动测试，直接比对 `expected.json` |
| 容器运行时镜像 | `gcr.io/distroless/static` | 无 shell、无包管理器，攻击面最小；静态编译产物可直接运行 |
| 编排 | Docker Compose | 题目要求一键启动 |
| 构建 | 多阶段 Dockerfile + `CGO_ENABLED=0` | 构建与运行分离，镜像只含产物 |

> **为什么用 distroless 而不是 alpine**：distroless 无 shell，无法写 `CMD wget` 式健康检查。解决方案是把健康探测做成**服务二进制的子命令**（`server healthcheck`），由镜像自身的二进制完成探测 —— 这既满足健康检测要求，又体现对镜像分层的理解。

---

## 三、总体架构

```
┌──────────────────────────────────────────────────────────────┐
│                        Docker Compose                        │
│                                                              │
│   ┌─────────────────┐         ┌───────────────────────────┐  │
│   │  client 容器     │  HTTP   │      server 容器           │  │
│   │                 │ ──────► │                           │  │
│   │  读 input.json  │  内网    │  ┌─────────────────────┐  │  │
│   │  POST 到 server │  仅暴露  │  │   api  (handlers)   │  │  │
│   │  打印/落盘结果   │  8080    │  └──────────┬──────────┘  │  │
│   └─────────────────┘         │             ▼             │  │
│          ▲                    │  ┌─────────────────────┐  │  │
│          │                    │  │  engine (匹配引擎)   │  │  │
│          │                    │  └──────────┬──────────┘  │  │
│          │                    │             ▼             │  │
│          │                    │  ┌─────────────────────┐  │  │
│          │                    │  │  rules  (YAML 规则)  │  │  │
│          │                    │  └─────────────────────┘  │  │
│          │                    │   healthcheck: 自探测      │  │
│          │                    └───────────────────────────┘  │
│          │                                                 │
│     frontend 网络（仅需要时）   backend 网络（internal: true）│
└──────────────────────────────────────────────────────────────┘
```

### 调用链

```
client → POST /fingerprint → api.Handler → engine.Fingerprint(batch)
        → rules 逐条匹配 → 组装 Result → JSON 响应 → client 打印
```

### 数据形态流转

```
原始 Banner(string, 含 \x00 \r\n 转义)
   │  normalize（归一化）
   ▼
规范化字节/字符串
   │  rule matching（正则 + 端口提示 + 协议特征）
   ▼
Match{protocol, product, version, os_hint, confidence}
   │  fallback
   ▼
unknown{protocol="unknown", confidence=0}
```

---

## 四、核心数据结构

### 4.1 输入 / 输出（对外契约）

```go
// 输入单条
type Record struct {
    IP     string `json:"ip"`
    Port   int    `json:"port"`
    Banner string `json:"banner"`
}

// 输出单条
type Result struct {
    IP         string  `json:"ip"`
    Port       int     `json:"port"`
    Protocol   string  `json:"protocol"`   // 认不出为 "unknown"
    Product    string  `json:"product"`
    Version    string  `json:"version"`
    OSHint     string  `json:"os_hint"`
    Confidence float64 `json:"confidence"` // 0~1
}
```

- `POST /fingerprint` 请求体为 `[]Record`，响应体为 `[]Result`（顺序与输入一一对应）。
- 输出字段零值语义：`product` / `version` / `os_hint` 用空串表示「无」，`protocol` 用 `"unknown"` 表示「认不出」。

### 4.2 规则模型（内部）

```go
type Rule struct {
    ID         string   `yaml:"id"`
    Protocol   string   `yaml:"protocol"`
    Product    string   `yaml:"product"`
    Priority   int      `yaml:"priority"`     // 越大越先匹配
    Ports      []int    `yaml:"ports"`        // 端口提示（弱约束）
    Banner     []string `yaml:"banner"`       // 特征正则（任一命中即匹配）
    VersionRe  string   `yaml:"version_regex"` // 含捕获组的版本正则
    OSRe       string   `yaml:"os_regex"`      // 可选的 OS 线索正则
    Confidence float64  `yaml:"confidence"`    // 基础置信度
}
```

---

## 五、识别引擎设计

### 5.1 匹配流程

1. **归一化（normalize）**
   - 将 banner 中字面量 `\r\n`、`\x00`、`\xNN` 还原为真实字符/字节。
   - 保留二进制安全性：非 UTF-8 字节不做强转，转为可安全匹配的副本（用于 MySQL 这类二进制 banner）。
   - 截断超长 banner，防止异常输入拖慢正则。

2. **规则筛选**
   - 按 `Priority` 降序排列规则。
   - 端口作为**弱提示**而非硬门槛：端口匹配可小幅提升置信度；端口不匹配不直接否决（因为服务可能跑在非标端口，如 8443 的 nginx）。

3. **特征匹配**
   - 遍历规则，任一 `Banner` 正则命中即视为候选。
   - 从 `VersionRe` 捕获组提取版本；`OSRe` 提取 OS 线索。

4. **择优**
   - 取置信度最高（并列时优先级最高）的候选。
   - 无候选 → `Result{Protocol: "unknown", Confidence: 0}`，其余字段留空。

### 5.2 置信度策略（对齐示例）

| 场景 | 置信度 | 说明 |
| --- | --- | --- |
| 协议 + 产品 + 版本 全部命中（SSH） | 0.95 | 信息最完整 |
| 协议 + 产品 + 版本 命中（HTTP/FTP/MySQL） | 0.90 | 标准命中 |
| 协议 + 产品 + 版本 命中（Jetty 等） | 0.85 | 规则专项分 |
| 仅协议特征命中，无版本 | 0.70 | Redis 类 |
| 完全认不出 | 0 | `unknown` |

> 置信度由规则显式声明，引擎仅做「端口一致性」的小幅修正，避免玄学打分。

### 5.3 覆盖的协议特征（规则要点）

| 协议 | 特征锚点 |
| --- | --- |
| SSH | `^SSH-\d+\.\d+-<Product>_<version>`，OS 从 `Ubuntu-x` / `Debian-x` 提取 |
| HTTP | `HTTP/\d\.\d \d{3}` + `Server:` 头 → 产品（nginx / Apache / Jetty / IIS）+ 版本 |
| MySQL | 二进制握手包 `J\x00...\n<version>\x00`，提取版本号 |
| Redis | `+PONG`、`-ERR ...`、`-NOAUTH ...`，无版本 |
| FTP | `220 ...` 首行 → ProFTPD / vsFTPd / Pure-FTPd / (无版本变体) |
| 其他 | 未命中 → `unknown`（如 TLS 握手 `\x16\x03\x01`、`QUIT\r\n`） |

---

## 六、规则解耦设计

**目标**：新增/调整指纹不改代码、不重建镜像。

- 默认规则通过 `go:embed` 编入二进制，保证「开箱即用」。
- 启动时若环境变量 `FINGERPRINT_RULES` 指向的文件存在，则**覆盖**内置规则。
- 规则文件可经由 Compose 的 volume 挂载进容器（只读），实现不改镜像换规则。
- 规则加载失败时：内置规则兜底 + WARN 日志，服务仍可启动（不因规则问题崩溃）。

```
优先级：外部规则文件  >  go:embed 内置规则
```

---

## 七、模块拆解

| 模块 | 路径 | 职责 | 对外接口 |
| --- | --- | --- | --- |
| server 入口 | `cmd/server` | 解析配置 → 加载规则 → 装配 engine/api → 启动 HTTP → 优雅关闭；实现 `healthcheck` 子命令 | `main()` |
| client 入口 | `cmd/client` | 读本地 JSON → 调用 server → 打印/落盘结果 | `main()` |
| model | `internal/model` | `Record` / `Result` 公共数据结构 | 结构体定义 |
| api | `internal/api` | 路由注册、请求解析、参数校验、错误响应、`/health` | `NewServer(engine) *http.Server` |
| engine | `internal/engine` | 归一化、规则匹配、择优、置信度修正、unknown 兜底 | `Fingerprint([]Record) []Result` |
| rules | `internal/rules` | 规则模型、加载（embed + 外部覆盖）、校验、排序 | `Load(path) ([]Rule, error)` |
| config | `internal/config` | 环境变量读取（端口、规则路径、日志级别、超时） | `Load() Config` |
| client 逻辑 | `internal/client` | HTTP 调用封装、结果渲染（便于单测） | `Run(cfg) error` |

**分层依赖方向（单向，无环）**：

```
cmd/* → internal/api → internal/engine → internal/rules
                 ↘ internal/model ↗
cmd/* → internal/config, internal/client
```

`engine` 不依赖 `api`，`rules` 不依赖 `engine`，保证可独立测试。

---

## 八、目录结构

```
.
├── README.md                    # 交付说明：启动方式、接口、规则扩展、设计取舍
├── 需求.md
├── 架构.md
├── go.mod
├── go.sum
├── Makefile                     # build / test / run / compose 快捷命令
├── docker-compose.yml           # 一键启动
├── .dockerignore
├── .gitignore
│
├── cmd/
│   ├── server/
│   │   └── main.go              # 含 healthcheck 子命令
│   └── client/
│       └── main.go
│
├── internal/
│   ├── api/
│   │   ├── router.go            # ServeMux 路由
│   │   ├── handler.go           # /fingerprint、/health
│   │   └── handler_test.go
│   ├── engine/
│   │   ├── engine.go            # 匹配主流程
│   │   ├── normalize.go         # banner 归一化
│   │   └── engine_test.go
│   ├── rules/
│   │   ├── rule.go              # Rule 模型 + 校验
│   │   ├── loader.go            # embed + 外部覆盖
│   │   ├── rules.yaml           # 默认规则（go:embed）
│   │   └── loader_test.go
│   ├── client/
│   │   └── client.go            # HTTP 调用 + 结果渲染
│   ├── config/
│   │   └── config.go            # 环境变量配置
│   └── model/
│       └── model.go             # Record / Result
│
├── deploy/
│   ├── Dockerfile.server        # 多阶段构建
│   └── Dockerfile.client
│
└── testdata/
    ├── input.json               # 示例输入（题目样例）
    └── expected.json            # 示例期望输出（用于自测对拍）
```

---

## 九、HTTP API 契约

### `POST /fingerprint`

- 请求：`Content-Type: application/json`，body 为 `[]Record`
- 响应：`200 OK`，body 为 `[]Result`（与输入等长、同序）
- 异常：
  - body 非法 JSON / 非数组 → `400`，返回结构化错误
  - 单条 banner 无法识别 → **不是错误**，该条返回 `unknown`
  - 内部 panic 由恢复中间件兜底 → `500`，服务不退出

### `GET /health`

- 响应：`200 OK`，`{"status":"ok","rules":<规则条数>}`
- 用于 Compose healthcheck 与人工探活

### 错误响应结构

```json
{"error": "invalid request body", "detail": "..."}
```

---

## 十、容器与部署设计

### 10.1 镜像（多阶段构建）

```
Stage 1 (builder):  golang:1.23-alpine
                    CGO_ENABLED=0 GOOS=linux go build -trimpath -ldflags="-s -w"
Stage 2 (runtime):  gcr.io/distroless/static:nonroot
                    COPY --from=builder /app/server /server
                    USER nonroot:nonroot
                    ENTRYPOINT ["/server"]
```

要点：
- `-trimpath -ldflags="-s -w"` 去符号、去调试信息，减小体积
- 静态编译产物 + distroless static，无 libc 依赖
- 默认以 `nonroot` 用户运行
- client 镜像同理独立构建（`deploy/Dockerfile.client`）

### 10.2 Compose 编排

| 关注点 | 做法 |
| --- | --- |
| 网络收敛 | 自定义 network `backend`，**不发布 server 端口到宿主机**；client 通过服务名 `http://server:8080` 访问 |
| 启动依赖 | `client.depends_on.server.condition: service_healthy`，等 server 真正健康再启动 |
| 健康检测 | server 容器 healthcheck 调用自身二进制 `["/server","healthcheck"]`（distroless 无 shell/wget） |
| 权限收紧 | `user: nonroot`、`read_only: true`、`cap_drop: [ALL]`、`security_opt: [no-new-privileges:true]`、`tmpfs: /tmp` |
| 可观测 | `slog` JSON 日志输出到 stdout，`docker compose logs` 可读 |
| 配置注入 | 端口/规则路径/日志级别通过 `environment` 注入，不写死在镜像 |

### 10.3 端口暴露策略

- server 仅在 `backend` 内网监听 `8080`，**不映射到宿主机端口**（访问方式收敛）。
- 若需人工验证，通过 `docker compose exec client ...` 或临时 `--profile debug` 暴露，默认关闭。

---

## 十一、健壮性与错误处理

| 风险 | 处理 |
| --- | --- |
| banner 含二进制/非法 UTF-8 | 归一化阶段安全处理，不 panic |
| banner 为空 / 字段缺失 | 直接判定 `unknown` |
| 超大 banner / 正则回溯 | 截断长度 + 使用非回溯型正则（避免 `.*` 贪婪嵌套） |
| 非法 JSON 请求体 | `400` + 结构化错误，不影响服务 |
| 规则文件损坏 | 回退内置规则 + WARN 日志 |
| handler panic | recover 中间件兜底，返回 `500`，进程不退出 |
| 批量请求部分失败 | 逐条独立识别，单条失败不影响整批 |

> 核心承诺：**任何输入都不会让服务崩溃**，`unknown` 是合法的、正常的输出。

---

## 十二、测试策略

| 层级 | 用例 |
| --- | --- |
| 单元测试 | `engine` 对 23 条示例逐条断言 `protocol/product/version/os_hint` |
| 规则测试 | `rules.Load` 校验去重、优先级排序、正则可编译 |
| 接口测试 | `api` 层用 `httptest` 验证 200 / 400 / unknown 兜底 |
| 端到端自测 | `testdata/input.json` → server → 与 `expected.json` 对拍 |
| 容器自测 | `docker compose up` 后 client 输出与预期一致 |

---

## 十三、实施优先级（30 分钟预算内）

| 优先级 | 内容 |
| --- | --- |
| P0（必做） | model / rules(内置 YAML) / engine(归一化+匹配) / api 两接口 / client / 两个 Dockerfile / compose / README |
| P1（保生产级） | distroless+nonroot、healthcheck、read_only/cap_drop、多阶段构建 |
| P2（加分） | 外部规则挂载覆盖、单测、`testdata` 对拍、结构化日志 |
| P3（有余力） | TLS 握手识别、更多产品（IIS 已含在规则） |

> 时间紧张时，先保证 P0 + P1 跑通，P2/P3 按剩余时间补齐。

---

## 十四、关键设计决策记录

| 决策 | 备选 | 结论与理由 |
| --- | --- | --- |
| HTTP 框架 | gin / chi / stdlib | **stdlib**：Go 1.22 ServeMux 已够用，零依赖降低构建风险 |
| 规则载体 | 硬编码 / JSON / YAML | **YAML + embed + 外部覆盖**：满足「规则与代码解耦」检查项 |
| 运行镜像 | alpine / distroless | **distroless**：无 shell、攻击面最小；健康检查改用二进制子命令 |
| server 端口 | 映射到宿主机 / 仅内网 | **仅内网**：体现「容器间访问收敛」 |
| 未知输入 | 报错 / unknown | **unknown**：题目硬性要求，不崩 |
| 置信度 | 动态计算 / 规则声明 | **规则声明 + 端口微调**：可解释、可对齐示例 |

---

## 十五、架构自检清单

- [ ] server 只暴露 `/fingerprint` 与 `/health`
- [ ] client 独立二进制，读 JSON → 调 server → 输出
- [ ] 规则存于 YAML，可外部挂载覆盖，程序不含硬编码指纹
- [ ] engine 与 api、rules 解耦，可独立单测
- [ ] 多阶段镜像 + 静态编译 + distroless + nonroot
- [ ] Compose：内网访问收敛、`service_healthy` 依赖、read_only、cap_drop
- [ ] 任意输入返回结构体，`unknown` 兜底，服务不崩
- [ ] README 说明启动、接口、规则扩展、设计取舍
