# Banner 指纹识别系统 — 需求与约束

## 一、项目目标

使用 **Golang** 开发一个 Banner 指纹识别系统。

系统接收一批网络扫描原始数据（`ip`、`port`、`banner`），识别出对应的**协议、软件与版本信息**，并以 **client + server 架构**、**Docker Compose 一键启动**的方式交付。

识别深度至少要达到示例所示（见第四节）。

---

## 二、功能需求

### 2.1 接口

server 需提供两个 HTTP 接口：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/fingerprint` | 批量识别，接收 banner 列表，返回识别结果列表 |
| `GET` | `/health` | 健康检查 |

### 2.2 client

- client 是**独立程序**
- 读取本地 JSON 文件（原始扫描数据）
- 将数据发送给 server
- 展示识别结果

---

## 三、数据规格

### 3.1 输入格式（原始扫描数据）

```json
[
  {"ip": "1.2.3.4", "port": 22, "banner": "SSH-2.0-OpenSSH_8.9p1 Ubuntu-3"},
  {"ip": "1.2.3.5", "port": 80, "banner": "HTTP/1.1 200 OK\r\nServer: nginx/1.24.0\r\nContent-Type: text/html"}
]
```

字段：`ip`、`port`、`banner`

### 3.2 输出格式（识别结果）

```json
[
  {"ip": "1.2.3.4", "port": 22, "protocol": "SSH", "product": "OpenSSH", "version": "8.9p1", "os_hint": "Ubuntu", "confidence": 0.95},
  {"ip": "1.2.3.5", "port": 80, "protocol": "HTTP", "product": "nginx", "version": "1.24.0", "os_hint": "", "confidence": 0.9}
]
```

### 3.3 输出字段定义

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| `ip` | string | 原样回填 |
| `port` | int | 原样回填 |
| `protocol` | string | 识别出的协议；**认不出来统一返回 `"unknown"`** |
| `product` | string | 软件名（如 nginx / Apache / OpenSSH） |
| `version` | string | 版本号；无法确定则为空字符串 |
| `os_hint` | string | 操作系统线索（如 Ubuntu / Debian）；无则为空字符串 |
| `confidence` | float | 置信度，取值 **0 ~ 1** |

---

## 四、识别能力要求（识别深度）

### 4.1 必须正确识别的协议

至少覆盖：**SSH、HTTP（nginx / Apache / Jetty）、MySQL、Redis、FTP**。

### 4.2 识别深度示例（自测数据，非最终验收数据）

对应输入 → 期望输出：

| ip | port | banner（原始） | protocol | product | version | os_hint | confidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1.2.3.4 | 22 | `SSH-2.0-OpenSSH_8.9p1 Ubuntu-3` | SSH | OpenSSH | 8.9p1 | Ubuntu | 0.95 |
| 1.2.3.5 | 80 | `HTTP/1.1 200 OK\r\nServer: nginx/1.24.0\r\nContent-Type: text/html` | HTTP | nginx | 1.24.0 | | 0.9 |
| 1.2.3.6 | 443 | `HTTP/1.1 200 OK\r\nServer: Apache/2.4.57` | HTTP | Apache | 2.4.57 | | 0.9 |
| 1.2.3.7 | 3306 | `J\x00\x00\x00\n8.0.32\x00` | MySQL | MySQL | 8.0.32 | | 0.9 |
| 1.2.3.8 | 6379 | `-ERR wrong number of arguments for 'get' command` | Redis | Redis | | | 0.7 |
| 1.2.3.9 | 21 | `220 ProFTPD 1.3.7 Server (ProFTPD)` | FTP | ProFTPD | 1.3.7 | | 0.9 |
| 1.2.3.10 | 8080 | `HTTP/1.1 404 Not Found\r\nServer: Jetty/9.4.51` | HTTP | Jetty | 9.4.51 | | 0.85 |

### 4.3 扩展识别场景（同一批数据中的其他形态）

| ip | port | banner | 期望 |
| --- | --- | --- | --- |
| 1.2.3.11 | 22 | `SSH-2.0-OpenSSH_9.3 Debian-1` | SSH / OpenSSH / 9.3 / Debian |
| 1.2.3.12 | 80 | `HTTP/1.1 200 OK\r\nServer: nginx/1.18.0 (Ubuntu)` | HTTP / nginx / 1.18.0 / Ubuntu |
| 1.2.3.13 | 443 | `HTTP/1.1 200 OK\r\nServer: Apache/2.4.41 (Ubuntu)` | HTTP / Apache / 2.4.41 / Ubuntu |
| 1.2.3.14 | 3306 | `J\x00\x00\x00\n5.7.42\x00` | MySQL / MySQL / 5.7.42 |
| 1.2.3.15 | 6379 | `+PONG` | Redis（无版本） |
| 1.2.3.16 | 21 | `220 (vsFTPd 3.0.5)` | FTP / vsFTPd / 3.0.5 |
| 1.2.3.17 | 8443 | `HTTP/1.1 200 OK\r\nServer: nginx/1.25.3` | HTTP / nginx / 1.25.3 |
| 1.2.3.18 | 22 | `SSH-1.99-OpenSSH_4.3` | SSH / OpenSSH / 4.3 |
| 1.2.3.19 | 9999 | `\x16\x03\x01\x00\xa5\x01\x00\x00\xa1` | unknown（不崩） |
| 1.2.3.20 | 8888 | `HTTP/1.1 200 OK\r\nServer: Microsoft-IIS/10.0` | HTTP / IIS / 10.0（建议） |
| 1.2.3.21 | 6379 | `-NOAUTH Authentication required.` | Redis |
| 1.2.3.22 | 21 | `220 Welcome to Pure-FTPd` | FTP / Pure-FTPd |
| 1.2.3.23 | 12345 | `QUIT\r\n` | unknown（不崩） |

> 说明：示例数据仅供自测，**不代表最终验收标准**。评估者另有一批未见过的验收数据，识别规则应具备一定泛化能力（端口 + banner 特征结合，而非仅硬编码端口）。

---

## 五、非功能需求（生产级部署）

整体用 **Docker Compose 一键启动**，按「生产级部署」标准发挥。评估者重点检查：

1. **容器间访问方式是否收敛** — 服务间通信路径清晰、收敛，不随意暴露。
2. **启动依赖是否有真实的健康检测** — 使用 healthcheck 等真实依赖判断，而非仅靠启动顺序。
3. **镜像体现的编译打包理解程度** — 多阶段构建、体积控制、构建产物组织。
4. **容器运行权限收紧程度** — 非 root 运行、只读文件系统、能力裁剪等。
5. **识别规则与程序代码是否解耦** — 指纹规则应可通过配置/规则文件扩展，而非全部硬编码在代码中。

> 不逐条列清单，**如何做到由实现者自行判断**。

---

## 六、交付要求

1. **交付形式**：可直接 `docker compose up` 启动的项目目录，包含源码、Dockerfile、compose 文件。
2. **自证能力**：提交后不再有现场演示或追问环节，代码本身要能自证 —— **注释、README 需写清楚**。
3. **GitHub 仓库**：需上传至可访问仓库（公开，或已加评估者账号为协作者），确保能直接克隆。

---

## 七、约束条件

1. **时限**：从收到题目起 **30 分钟内**完成开发。时间截止后，无论完成度如何，都需将源码上传至 GitHub 仓库并回发仓库地址。**评估基于提交时的实际完成情况，不接受截止后再补提交。**
2. **健壮性**：认不出来统一返回 `protocol: "unknown"`，**不允许因为认不出来就报错或崩溃**。
3. **评估方式**：评估者离线克隆代码，使用另一批未见过的验收数据测试。
4. **过程约束**：本次 AI Coding 过程中，**不允许使用人工编程方式方法**，全程需共享屏幕并录屏。
5. **操作约束**：操作 GitHub、中间件、运行环境、外部资源等，均与 coding 一样，**不允许手动操作和人工编辑**，唯一入口是通过 AI 编程工具/平台输入提示词。

### 隐含约束

- GitHub 仓库的上传与配置也需通过 AI 工具完成（对应第 5 条）。
- 项目需保证在评估者的干净环境中可复现（离线克隆 + `docker compose up` 即可运行）。

---

## 八、验收要点汇总

- [ ] `POST /fingerprint` 批量识别接口可用
- [ ] `GET /health` 健康检查接口可用
- [ ] client 独立程序：读取本地 JSON → 调用 server → 展示结果
- [ ] 输出含全部 7 个字段，`confidence` 取值 0~1
- [ ] 无法识别时返回 `protocol: "unknown"`，且服务不崩溃
- [ ] 正确识别 SSH / HTTP(nginx、Apache、Jetty) / MySQL / Redis / FTP
- [ ] `docker compose up` 一键启动
- [ ] 容器间访问收敛、有健康检测、镜像多阶段构建、权限收紧、规则与代码解耦
- [ ] README 与注释完整，代码可自证
- [ ] 源码提交至可访问的 GitHub 仓库
